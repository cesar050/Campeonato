/*export const environment = {
  production: true,
  apiUrl: 'https://tu-dominio.com',
  tokenKey: 'access_token',
  refreshTokenKey: 'refresh_token',
  userKey: 'current_user'
};*/
export const environment = {
  production: true,
  apiUrl: 'https://e9ed52b9025b.ngrok-free.app',  // ← URL de ngrok del backend
  tokenKey: 'access_token',
  refreshTokenKey: 'refresh_token',
  userKey: 'current_user'
};